# 飞书 Agent 卡片

AstrBot 飞书回复展示插件。收到 Agent 请求后立即创建卡片，在同一张卡更新处理状态、正文、工具摘要、来源和真实模型用量。

当前版本 **0.2.12**，仅支持 AstrBot **4.28.0** 的 `lark` 官方适配器。交互及终止按钮需要 **socket 长连接模式**和卡片回调订阅。采用 [MIT 许可证](LICENSE)。

## 安装与启停

1. 在 AstrBot 的「插件」页面选择通过 GitHub 仓库地址安装，填写：

   ```text
   https://github.com/gobelieve0905/astrbot_plugin_feishu_card
   ```

2. 确认使用 AstrBot 4.28.0，已配置飞书机器人和支持工具调用的模型。
3. 在飞书开放平台为同一应用开通发送消息、创建/更新卡片的权限；使用图片时开通图片资源上传权限。具体权限以对应 API 的权限列表为准：
   - [创建卡片](https://open.feishu.cn/document/cardkit-v1/card/create)
   - [更新卡片](https://open.feishu.cn/document/cardkit-v1/card/update)
   - [上传图片](https://open.feishu.cn/document/server-docs/im-v1/image/create)
4. 在「事件与回调 → 回调配置」使用长连接，订阅 `card.action.trigger`，并按后台提示发布应用版本。
5. 在插件设置中开启「飞书 Agent 卡片」。若 Agent 配置了工具白名单，加入 `feishu_card_guide`、`feishu_card_render`、`feishu_card_upload_image`。
6. 发送普通问题验证卡片，再尝试“用表格和柱状图展示一组示例数据，并生成补充需求表单”。提交表单后应进入原生消息管道并发送新卡片；生成过程中可点击“终止回答”。

在 AstrBot 插件管理中找到「飞书 Agent 卡片」，修改配置、重载或禁用。无需新建飞书应用、填写第二套密钥、安装系统服务或修改 AstrBot 源码。

插件复用已配置飞书客户端，需要当前应用具备 CardKit 创建/更新卡片及回复消息的权限。开通权限后仍须按照飞书应用流程发布生效。创建卡片失败时保留原生回复；运行中更新失败时继续消费模型输出，在收尾阶段重试并补发普通正文。

## 展示与行为

- 即时卡片在 AstrBot 已决定进入 LLM 的等待钩子创建，普通未唤醒群消息不触发。
- 处理过程：宿主真实生命周期、工具开始/返回、请求重试及模型切换。没有额外模型调用，不展示原始 reasoning。
- 正文：同卡定时更新（默认 1.5 秒），工具阶段保留当前卡片。超过约 10 KB 正文后续页；不截断最终内容。代码围栏跨页会补齐显示边界；极长表格建议使用原生 table 组件分页。
- 当前会话的 `send_message_to_user` 纯文本纳入已有卡片，重复文本去重，最终模型正文优先；媒体、跨会话发送和未接管会话保持原流程。
- 工具：优先显示提供方声明的自定义名称、耗时、返回/失败状态，不输出参数、原始结果或供应商错误正文。
- 来源：成功工具名称显示为「工具结果」；只有明确来源合同才展示文档链接，不把随意出现的 URL 当成引用。
- 用量：只累计每个模型请求的非 chunk 终态用量，含缓存输入；失败或缺失用量会标明统计不完整。未返回的费用/Token 不推算。
- 费用：可选管理员维护价格。未设置时不显示费用；展示的是按已返回用量估算，不是供应商账单。
- 模型无正文：显示“未生成正文”；如工具失败，展示工具名称和失败状态，不将思考内容或原始接口错误作为回答。
- 失败/取消：明确终态。插件停用会关闭自身卡片并恢复原有发送方式。
- 重启：仅保存未完成卡片 ID、平台 ID、序号和时间；启动后尝试将遗留卡片标为中断，不自动重放原请求。原文、业务结果、密钥不落入插件数据。

**会话规则保持 AstrBot 原有设置。** 本版不实现普通引用消息新建/恢复会话或运行中追加指令，不会写出尚未实现的交互提示。普通附件仍由原生适配器发送。

## 配置

“生效的飞书实例”使用多选下拉框，只需勾选，无需手填 ID；留空表示全部飞书实例。选项包括已配置但暂时停用的飞书实例，已有选择不会因实例删除而自动清空。新增/删除实例后，等待约 10 秒并重新打开配置窗口。

“图片上传允许目录”限制插件从服务器哪些文件夹读取并上传图片，不是飞书保存图片的位置。一般留空即可，默认允许 AstrBot 的 `data/temp` 和 `data/plugin_data`。只有生成图片放在其他目录时才需自定义；Docker 应填容器内绝对路径，自定义列表会替换默认目录。

插件后台支持总开关、实例 ID 列表、私聊/群聊开关、过程/工具/来源/用量可见性、过程默认折叠、更新间隔和模型价格。

价格 JSON 以卡片实际展示的 `供应商标识/模型` 为键。每个模型提供每百万 Token 的 `input`、`cached_input`、`output` 三项单价。币种由 `price_currency` 指定；不做汇率换算。价格缺失或格式无效时隐藏费用。

“群聊使用卡片”只决定回复的展示方式。是否需要 @机器人、使用唤醒词以及用户权限，仍由 AstrBot 管理，开启后不会自动回复全部群消息。

“模型 Token 计价币种”是下方单价及估算结果的币种，例如 USD（美元）、CNY（人民币）。只设置币种不会自动查询价格，也不会做汇率换算。所有模型价格应使用同一币种。

填写示例（**以下数字仅演示格式，不代表真实模型价格**）：

```json
{
  "provider-id/model-name": {
    "input": 1.0,
    "cached_input": 0.1,
    "output": 2.0
  }
}
```

将 `provider-id/model-name` 替换为回复卡片显示的完整模型标识。`input` 是每百万普通输入 Token 单价，`cached_input` 是每百万缓存输入 Token 单价，`output` 是每百万输出 Token 单价；填写供应商实际价格。如有多个模型，为每个完整标识分别添加配置。留空或 `{}` 不显示费用；还需开启“显示模型与用量”，且本轮返回用量、对应价格齐全，才能估算。


## 来源扩展合同

其他插件可向当前 `event` 登记实际取回、允许在当前会话展示的来源：

```python
event.set_extra("feishu_card_sources", [
    {"title": "文档标题", "url": "https://example.com/doc", "type": "知识库"}
])
```

MCP 工具结果的 `structuredContent.sources` 支持同样结构。本插件不改变工具授权，也不声称所有知识库实现均已自动适配。来源最多 20 项，按标题/链接/类型去重。HTTPS URL 不允许凭据、查询参数、fragment、本地地址或明显令牌路径；无法安全展示时只保留标题。调用方负责来源真实性和当前用户权限。

## 兼容实现

公开钩子负责等待、请求、Agent 生命周期及工具事件；每个事件实例独立绑定发送方法。仅为读取逐请求用量/重试/最终错误，临时包装 v4.28.0 的两个模型迭代方法。包装器继续调用原实现，不改模型选择、重试次数、业务推理或工具调用。方法恢复有所有权检查；不兼容版本停用扩展并保留原生回复。

不要同时启用其他接管飞书流式发送的插件；与其他插件的组合兼容需要单独验证。当前模型重试策略完全沿用宿主，本插件显示等待，不改变上游可用性。

## 验证

纯渲染测试可在普通 Python 环境以 `python -B -m unittest discover -s tests -p test_card.py` 运行。完整测试需要 AstrBot 4.28.0 环境：`python -B -m unittest discover -s tests -v`。测试使用明确标记的测试事件和模拟传输，仅验证机制，不代表真实业务验收。

服务器验收另执行真实飞书 CardKit 创建/更新/终态结构检查，但不向任何用户投递测试消息。最终视觉与业务交付由用户通过当前飞书机器人验收。

## 0.2.7 展示层次

正式回答为独立正文区，以分隔线、加粗标题和正常字号突出；辅助过程、工具和来源采用浅灰折叠面板与明确箭头。模型用量、耗时和底部提示使用灰色小字。工具边界前的模型公开说明归入处理过程，宿主工具状态不进入正文；最终正文以宿主最终回答为准，不使用关键词识别业务内容。

卡片不重复引用原问题，保留飞书原生回复引用。正文标题优先使用模型在最终回答开头生成的主题标题，并以标题字号展示；无标题时以截短的问题文本兜底。不额外调用模型，用户明确要求的输出格式优先。旧版 show_question 配置不再生效。

展示顺序：主题标题、正式回答、分隔线、处理过程、工具调用、知识与数据来源、模型用量及提示。


## Agent 原生组件工具（0.2.7）

- `feishu_card_guide`：按场景查询原生组件与代码、表格、图表、图片、多栏、按钮、表单示例。
- `feishu_card_render`：接收完整 **JSON 2.0** 文档及完整文字兜底，先创建未发送的卡片校验结构，再更新当前回复。原生 header、body、组件及嵌套字段保留；辅助过程、来源、用量仍在底部。模型最终的“已生成卡片”不会覆盖内容。
- `feishu_card_upload_image`：将当前任务已生成的 PNG/JPEG/GIF/WebP 上传成真实 img_key；默认仅允许 data/temp 和 data/plugin_data，管理员可配置 image_roots。文件限 10 MB，不下载远程 URL。

组件入口不按 tag 白名单限制，飞书新增的合法 JSON 2.0 组件可直接使用；这不代表所有组件、客户端或组合均已逐项验收。卡片字节数、组件数量、应用权限和客户端能力仍由飞书决定。当前文档限 24 KB，复杂内容应拆分为可切换的视图，不能静默裁剪业务数据。JSON 1.0 与搭建平台模板不是本工具的输入格式，需要先转换或解析为 JSON 2.0；音视频和文件附件仍由 AstrBot 原生消息能力发送。

### 交互继续处理

飞书应用后台：**事件与回调 → 回调配置 → 使用长连接接收回调 → 添加“卡片回传交互（card.action.trigger）”**，按后台要求保存并发布应用。只订阅“接收消息”不够。

为原生组件填写 `behaviors: [{"type": "callback", "value": {"action": "希望继续处理的任务"}}]`。表单提交按钮还需 `form_action_type: "submit"`。插件记录原提问身份；提交的表单、选择值作为普通用户输入，通过 AstrBot 标准消息管道处理并发送新卡片，保留原卡片内容。不执行任意回调 URL，也不把输入当作系统指令。

**交互卡片生成时会直接显示使用说明**：同一张卡片所有人合计只能成功提交一次，允许同群成员不等于每个人都能各提交一次。表单与按钮绑定只保存在内存中，生成后 24 小时过期，插件重载或重启后立即失效。该提示始终显示，不藏在折叠区，也不受过程/来源显示开关影响；普通无续聊控件的卡片不加提示。

按钮失效不会删除已有话题记录。使用支持引用续聊的话题功能时，可以引用原消息继续讨论；也可以要求 Agent 重新生成表单。**独立安装本插件不会自动提供话题保存或引用恢复能力**，普通连续会话仍由 AstrBot 管理。终止与下载按钮使用各自规则，不受表单的一次性提交限制。

后台“群聊继续操作权限”：`initiator` 表示仅原发起人（默认），`members` 表示同群成员。私聊始终仅原用户；必须在原聊天操作，转发至其他聊天的续聊按钮被拒绝。此配置与终止回答、下载权限独立。其他成员操作时，消息发送者为实际点击者，仍经过 AstrBot 原生权限检查。Agent 忙碌时提示等待，同一视图只接受一次操作；相同回调的重复投递不重复执行。链接跳转行为仍由飞书处理。绑定仅在内存保存，24 小时过期；插件重载或重启后旧交互需重新生成。当前支持 AstrBot 4.28.0 **socket** 适配器；webhook 模式或已有其他插件占用回调处理器时拒绝生成回调型卡片，避免静默失效。

验收建议：要求 Agent 展示一段 Python 代码、用表格和柱状图展示测试数据、再生成一个补充需求表单；提交后检查是否按 AstrBot 原生会话规则发送新卡片，且原卡片内容不变。需启用本插件工具（如 Agent 使用工具白名单，应加入上面三个名称）。插件不自动发送验收消息。


### 续聊事件约定（0.2.8）

这是插件之间新增的约定，**不是 AstrBot 内置标准**。插件在提交事件前设置 `conversation_continuation_v1`，精确包含 `version`、`source`、`event_id`、`origin_message_id` 四个字段；详见 [续聊事件约定](https://github.com/gobelieve0905/astrbot_plugin_feishu_card/blob/main/CONTINUATION_V1.md)。新交互不复用原提问消息编号，同一回调重复投递使用相同编号。

插件不查话题数据库、不切换对话、不导入或检测任何话题插件。单独安装时使用 AstrBot 原生连续会话；如需点击旧卡片恢复对应项目，由其他消费者根据本约定和可信事件身份自行实现。仅提示“已接收操作，已提交 AstrBot 处理”，不表示某个话题已恢复。当前卡片插件代码完成协议生产端；未替其他插件实现消费端，也未验证其恢复行为。

已核实 AstrBot **4.28.0** / `lark-oapi` **1.7.3** 的 `LarkPlatformAdapter.create_event()` → `commit_event()` 入口。原生群会话使用群 ID，私聊使用实际用户 ID；不复制原事件可能被其他插件改写的 `session` 或管理员/唤醒状态。显示、终止、下载和原生发送回退保持可用。回调绑定只驻留内存，重启或重载后明确提示过期，本次不恢复旧交互。

### 终止回答（0.2.7）

生成中卡片底部自动显示“终止回答”（需已配置的 socket 卡片回调）。私聊始终仅发起人可操作。群聊由 `group_stop_initiator_only`（后台“群聊终止仅限发起人”）控制，默认开启；关闭后允许同群成员终止，仍拒绝其他聊天或缺少聊天信息的非发起人回调。此设置仅影响终止按钮，表单及继续处理按钮由独立的“群聊继续操作权限”控制。点击后显示“正在终止”，通过 AstrBot 原生 agent_stop_requested 信号中止当前模型/工具等待，保留已输出内容并标记“已终止”，随后移除按钮。重复点击不会启动新的任务。普通回复与原生组件卡片均支持。

停止不会撤销已经完成的外部操作；远端工具是否实际取消由工具自身决定，插件仅停止本轮执行和后续输出。


## 常见问题

- **不兼容版本**：当前使用 4.28.0 的部分运行时接口；其他版本不会启用接管，请勿直接放宽版本约束。
- **按钮无响应**：确认订阅的是“回调配置”中的 `card.action.trigger`，使用长连接，并完成应用发布。重载或重启后的旧按钮会过期，需要生成新卡片。
- **没有费用**：需开启用量显示、模型返回用量，并为本轮全部模型填写价格。不会自动查询供应商账单。
- **安装后出现重复卡片或输出**：检查是否同时启用了其他接管飞书流式输出的插件。
- **升级或禁用**：通过 AstrBot 插件管理操作。卸载或重载会使旧交互绑定失效；旧卡片内容不会被删除。

问题反馈：[GitHub Issues](https://github.com/gobelieve0905/astrbot_plugin_feishu_card/issues)。请提供 AstrBot 版本、插件版本、复现步骤和脱敏日志，不要上传密钥、Token 或业务数据。


### 横向宽表与回复下载（0.2.7）

原生 table 按内容设置像素列宽与自动行高，保留原生表格，不追加逐行明细。总列宽超出可用区域时使用飞书原生表格溢出展示；横向滑动手势、滚动条及长单元格换行效果由客户端决定，需要在目标客户端验收，插件不能注入 CSS 或强制客户端滚动。极长字段仍受客户端布局限制，可下载完整数据。

后台“允许下载回复”（`enable_reply_download`）默认开启。回答结束后只显示 **下载回复 (.md)** 按钮，不自动发文件。点击后，机器人将 **Agent回复.md** 发送到点击者的私聊，使用飞书原生文件下载功能保存。不会启动 Agent 或改写原卡片，也不会向群里群发附件。

所有能看到卡片的用户（包括转发卡片的查看者）均可点击，不限提问者；仍受飞书应用可用范围和组织文件策略约束。文件包含完整正文/文字兜底、代码、原生表格的 Markdown 表格和图表数据；图片仍为原回复中的引用，不嵌入二进制。终止回答包含已输出内容及状态，不包含隐藏推理、工具原始参数或回调凭据。

下载内容仅在内存缓存，24 小时有效，最多 256 份 / 总计 16 MB，超限淘汰旧记录。插件重载或重启后旧下载按钮失效；已下载文件不受影响。关闭开关会拒绝后续下载。需要应用文件上传和向用户发送消息权限；同一用户短时间重复点击会合并，其他人仍可独立下载。

插件独立运行，不依赖 API 接入或任何业务插件。只读取宿主标准工具结果的 isError，不解析工具正文中的业务字段；可选显示名称缺省时使用内部工具名称。

### JSON 输入兼容（0.2.12）

卡片工具兼容完整 JSON 代码围栏、有限层重复序列化，以及字符串内未转义的换行/制表符；保留原始文字、代码和表格数据。缺失引号、截断、非法转义等歧义结构仍拒绝并返回行列位置，不猜测或删减业务内容。建议 Agent 用标准 JSON 序列化一次生成 card_json。此兼容仅适用于已到达卡片工具的参数，不修复宿主尚未解析成功的外层工具调用参数。
