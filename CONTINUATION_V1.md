# conversation_continuation_v1 续聊事件约定

状态：新增的插件间约定，非 AstrBot 内置标准。本文规定卡片插件生产端行为；消费端须使用完全一致的字段名和语义。需要更改时须明确更新协议并协调双方，不能静默改名。当前没有更改用户给定的四字段结构。

## 数据结构与时序

校验真实 SDK 回调、原聊天、原平台与操作权限之后，创建原生事件，再在 `platform.commit_event(event)` 前调用：

```python
event.set_extra("conversation_continuation_v1", {
    "version": 1,
    "source": "card_interaction",
    "event_id": "card_interaction_<sha256>",
    "origin_message_id": "原用户提问的真实飞书消息编号"
})
```

| 字段 | 类型 | 语义 |
| --- | --- | --- |
| version | integer | 固定为 1 |
| source | string | 固定为 card_interaction |
| event_id | string | 本次交互的唯一编号，与 event.message_obj.message_id 完全一致 |
| origin_message_id | string | 创建这张卡片所对应的原用户提问编号；来自内存绑定的可信原事件 |

`event_id` 是 `card_interaction_` 前缀，加对 `[平台实例 ID, 机器人 ID, SDK header.event_id]` 的 JSON 数组（Python json.dumps，ensure_ascii=False）UTF-8 编码计算的 SHA-256 十六进制摘要。消费者应将其当作不透明字符串。它不是可供飞书消息 API 直接寻址的消息编号，且不等于原提问编号。不同真实回调产生不同编号；同一回调重投编号相同。缺失有效 SDK header.event_id 时明确拒绝，不能从表单/按钮载荷补齐，也不临时随机生成编号。

生成卡片时必须在可见正文中显示一次性提交与有效期提示，不等待过期或提交后才告知。同群权限开放后，仍是所有人合计一次，而非每人一次。

同一张卡片视图保持一次有效操作语义：成功入队后该视图其他操作不再执行；原回调重投提示已接收，不再次入队。同步提交前保留占位，入队失败回滚占位，使相同回调可重试。卡片绑定最多 256 份，24 小时过期，重载/重启清空；失效绑定被拒绝，不能通过旧回调重建上下文。这里保证插件入队去重，不承诺外部工具事务的 exactly-once 语义。

对由续聊生成的新卡片，插件自身的可信发送锚点会继续指向最初的真实用户提问。不会把合成交互编号伪装成真实飞书消息编号，也不会从用户提供的 extras 恢复锚点。

## 身份与信任边界

- 平台来自安装了本回调处理器的适配器实例，并与绑定的平台对象、实例 ID 比较。
- 聊天来自回调 context.open_chat_id，并必须与创建卡片时记录的原聊天完全相同。私聊同样检查；缺失聊天信息拒绝。
- 机器人来自原事件的 get_self_id()，并在适配器提供 bot_open_id 时核对。SDK header.app_id 提供时，与适配器 appid 比较。
- 实际用户来自回调 operator.open_id。群聊可配置为仅原发起人（默认）或同群成员；私聊始终仅原用户。event.message_obj.sender 不复制原提问者，设置为实际操作人，昵称在未查询真实姓名时使用其 open_id。
- 表单字段和按钮 value 中的 event_id、origin_message_id、会话 ID、发送者、平台或同名 continuation 字段均只作为普通用户数据。它们不能覆盖元数据和事件身份，消费端不得把用户正文里的同名 JSON 作为路由依据。
- 来源保证由已配置飞书 SDK 长连接和平台绑定提供；本插件不提供公开的未经验证回调入口，也不建立第二条长连接。

## 标准管道与独立运行

核实版本：AstrBot 4.28.0，lark-oapi 1.7.3。使用 `LarkPlatformAdapter.create_event(AstrBotMessage)` 创建 `LarkMessageEvent`，使用 `commit_event()` 的原生事件队列提交。新消息的外层 message_id 和 raw_message.message_id 都是新编号；不会留下旧提问正文、旧引用 parent_id 或旧发送者。

群聊原生 session_id 为群 ID，私聊为实际用户 ID。事件包含真实机器人 At 组件，交给原生唤醒和权限阶段；不继承原事件的管理员权限、唤醒标记、被改写后的 session 或其他 extras。元数据仅表达“这是哪条提问的后续操作”。插件不访问话题数据库、不选择/切换 conversation、不导入或检测其他话题插件。

没有消费者时，事件按 AstrBot 原生连续会话处理，不保证群内旧项目话题恢复。有消费者时，其应结合可信平台、机器人、聊天、操作人及 origin_message_id 验证和选择讨论，严格读取本文的四字段约定。消费端成功恢复话题之后才有资格提示恢复成功；卡片回调仅提示“已接收操作，已提交 AstrBot 处理”。

## 回复发送与生命周期

AstrBot 4.28.0 的飞书发送接口使用 message_id 作为回复目标。因此本插件只为该合成事件安装实例发送包装：消息管道始终看见新编号，发送用的独立对象指向绑定中的真实原提问编号。包装涵盖普通发送、原生流式回退以及插件卡片发送；不会临时把共享输入事件编号改回旧编号，不修改 AstrBot 类或核心文件。Session 收尾恢复时仍保留该事件的发送锚点，避免插件失败/停用后回退发送合成编号。

续聊产生新的卡片，旧卡片正文不被覆盖。交互管理器关闭后不再重新安装处理器，Session 收尾尝试创建下载绑定也不会重新开启回调。终止权限、下载权限与本续聊权限独立。重启恢复旧卡片交互不属于此协议实现范围。

## 验证范围

网络隔离的 AstrBot 4.28.0 运行测试覆盖标准 create_event/commit_event 队列入口、新编号/重投稳定性、队列失败重试、实际发送者、群聊/私聊权限、跨聊天拒绝、应用和机器人不匹配、伪造载荷不能覆盖元数据、原生会话和回复发送锚点、重载过期，以及已有展示/取消/下载/回退回归。没有导入话题插件或恢复真实业务话题，没有向真实用户发送测试消息。
